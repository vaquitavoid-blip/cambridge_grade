# Cambridge Economics AI Grader - Dependencies
# Install with: pip install -r requirements.txt

# Core ML / Fine-tuning
transformers==4.40.0
datasets==2.19.0
torch==2.2.2
accelerate==0.29.3
peft==0.10.0          # LoRA fine-tuning (memory efficient)
bitsandbytes==0.43.1  # 4-bit quantization for lower RAM usage
trl==0.8.6            # Supervised Fine-Tuning trainer

# Data handling
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
openpyxl==3.1.2       # Excel support for essay spreadsheets

# Evaluation & metrics
rouge-score==0.1.2
evaluate==0.4.1

# UI / CLI
rich==13.7.1           # Pretty terminal output
questionary==2.0.1     # Interactive CLI prompts

# Utilities
python-dotenv==1.0.1
tqdm==4.66.2
matplotlib==3.8.4      # Graphs in notebooks
seaborn==0.13.2

# Jupyter (optional, for notebooks)
jupyter==1.0.0
ipykernel==6.29.4

# OCR — for reading PDFs and images
pdfplumber==0.11.0        # Extract text from digital PDFs (no OCR needed)
PyMuPDF==1.24.3           # Convert scanned PDF pages to images for OCR
pytesseract==0.3.10       # OCR engine wrapper (also needs Tesseract installed separately)
Pillow==10.3.0            # Image processing

# Web app
streamlit==1.33.0         # Web interface
plotly==5.21.0            # Charts in the dashboard

# Google Sheets backend (for crowdsourced essay submissions)
gspread==6.1.2
google-auth==2.29.0